package org.example;

public class As400RPGSourceFile extends As400AbstractSourceFile implements As400SourceFile{
    As400RPGSourceFile(String localPath, String remotePath, String remoteLibrary) {
        super(localPath, remotePath, remoteLibrary);
    }
}
