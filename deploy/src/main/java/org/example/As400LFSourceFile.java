package org.example;

public class As400LFSourceFile extends As400PFSourceFile {
    As400LFSourceFile(String localPath, String remotePath, String remoteLibrary, String remoteLibraryFile, String remoteMBRPath, String recordName) {
        super(localPath, remotePath, remoteLibrary, remoteLibraryFile, remoteMBRPath, recordName);
    }
}
